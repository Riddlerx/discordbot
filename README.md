# discordbot

## YouTube auth for music playback

The bot uses the browser profile directly (`/home/ubuntu/.youtube-profile`) for authentication, which is configured in the service environment. No manual cookie file management is required.
